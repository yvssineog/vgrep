{
  "name": "vgrep",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun sst dev",
    "test": "bun test --recursive",
    "lint": "tsc --noEmit",
    "vgrep": "bun packages/cli/src/cli.ts",
    "bench": "bun packages/bench/src/run.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/bun": "latest",
    "sst": "latest"
  },
  "dependencies": {
    "@types/aws-lambda": "^8.10.161",
    "aws-lambda": "^1.0.7"
  }
}